import { Brands_KPIs, Layout } from '@/index';

export default function BrandsPage() {
    const title = 'Brands KPIs';

    return (
        <Layout title={title}>
            <Brands_KPIs />
        </Layout>
    )
}