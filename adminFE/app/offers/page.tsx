import { Layout, Offers } from "@/index";

export default function OffersPage() {
    const title = 'Offers';

    return (
        <Layout title={title}>
            <Offers />
        </Layout>
    )
}
