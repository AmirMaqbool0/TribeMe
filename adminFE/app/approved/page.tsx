import { Approved, Layout } from "@/index";

export default function ApprovedPage() {
    const title = 'Approved';

    return (
        <Layout title={title}>
            <Approved />
        </Layout>
    )
}